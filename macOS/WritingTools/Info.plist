<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <key>CFBundleIdentifier</key>
        <string>com.aryamirsepasi.writing-tools</string>
        <key>CFBundleName</key>
        <string>WritingTools</string>
        <key>CFBundleDisplayName</key>
        <string>WritingTools</string>
        <key>CFBundleExecutable</key>
        <string>$(EXECUTABLE_NAME)</string>
        <key>CFBundlePackageType</key>
        <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
        <key>CFBundleShortVersionString</key>
        <string>$(MARKETING_VERSION)</string>
        <key>CFBundleVersion</key>
        <string>$(CURRENT_PROJECT_VERSION)</string>
        <key>LSMinimumSystemVersion</key>
        <string>$(MACOSX_DEPLOYMENT_TARGET)</string>
        <key>LSUIElement</key>
        <true />
        <key>NSAccessibilityUsageDescription</key>
        <string>WritingTools needs access to control your computer to simulate copy and paste actions, allowing it to retrieve selected text and insert AI-generated results.</string>
        <key>NSScreenCaptureUsageDescription</key>
        <string>WritingTools uses screen capture to read text from images on your screen via OCR.</string>
        <!-- Allow plain-HTTP connections to user-configured AI endpoints
             (e.g. a llama.cpp / Ollama server at http://host:port, including
             Tailscale IPs like 100.x.y.z). App Transport Security otherwise
             blocks any non-HTTPS request.
             NOTE: do NOT also add NSAllowsLocalNetworking here — on modern
             macOS its presence makes NSAllowsArbitraryLoads be IGNORED, which
             re-blocks non-local HTTP hosts (e.g. Tailscale IPs). -->
        <key>NSAppTransportSecurity</key>
        <dict>
            <key>NSAllowsArbitraryLoads</key>
            <true />
        </dict>
    </dict>
</plist>